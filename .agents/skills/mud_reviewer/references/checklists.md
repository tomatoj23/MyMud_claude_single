# 代码审查清单

## Typeclass系统检查

- [ ] `typeclass_path` 类属性正确定义
- [ ] `AttributeHandler` 实现属性代理
- [ ] `db` 属性访问透明
- [ ] 动态加载使用 `importlib`
- [ ] 生命周期钩子可重写：`at_init`, `at_delete`, `at_move`
- [ ] 脏数据标记机制正确
- [ ] 使用 `weakref` 避免循环引用

## ObjectManager检查

- [ ] L1缓存使用 `weakref` 避免循环引用
- [ ] L2缓存使用 `functools.lru_cache`
- [ ] `get()` 方法优先从L1获取
- [ ] `save()` 标记脏数据机制
- [ ] `find()` 支持按类型/位置查询
- [ ] 批量保存机制实现

## Command系统检查

- [ ] `Command` 基类有 `key`, `aliases`, `locks`
- [ ] `CmdSet` 支持优先级合并
- [ ] Trie树前缀匹配实现
- [ ] 权限锁系统可扩展
- [ ] 命令解析流水线完整
- [ ] 错误处理完善

## EventScheduler检查

- [ ] 使用 `asyncio` 而非 `Twisted`
- [ ] 支持延迟/周期/条件/帧事件
- [ ] 优先级队列使用 `heapq`
- [ ] 时间膨胀可调 `set_time_scale`
- [ ] 异常处理不影响其他事件
- [ ] 事件ID唯一性检查

## GameEngine检查

- [ ] 正确初始化所有子系统（数据库、对象管理器、命令、事件）
- [ ] 启动/关闭流程正确
- [ ] 主循环定期保存脏数据
- [ ] 输入处理异常捕获

## 角色系统检查

- [ ] Character三层属性结构（先天/后天/动态）
- [ ] 属性计算正确（气血/内力公式）
- [ ] 门派系统Mixin正确混入
- [ ] 内力系统Mixin正确混入
- [ ] 经脉系统实现正确

## 装备系统检查

- [ ] 12+装备槽位定义完整
- [ ] 属性加成计算正确
- [ ] 耐久度系统工作正常
- [ ] 套装效果计算正确
- [ ] 装备条件检查（等级/门派）

## 武学系统检查

- [ ] 招式效果脚本沙箱安全
- [ ] 克制关系计算正确
- [ ] 学习条件检查完整
- [ ] 熟练度系统工作正常
- [ ] 武功升级逻辑正确

## 地图系统检查

- [ ] 三维坐标系统正确
- [ ] 出口双向连接正确
- [ ] 动态加载策略实现
- [ ] A*寻路算法正确

## 战斗系统检查

- [ ] 回合制/即时制双模式支持
- [ ] 伤害计算公式正确
- [ ] 命中率/闪避率计算正确
- [ ] BUFF/DEBUFF系统工作正常
- [ ] 战斗AI有基本策略

## 任务系统检查

- [ ] 任务目标追踪正确
- [ ] 前置条件检查完整
- [ ] 奖励发放正确
- [ ] 任务链连接正确

## NPC系统检查

- [ ] 行为树执行正确
- [ ] 日常行程系统工作
- [ ] 好感度系统正确
- [ ] 对话树无死循环

## 存档系统检查

- [ ] MessagePack序列化正确
- [ ] Gzip压缩/解压正常
- [ ] 存档版本兼容性处理
- [ ] 自动存档触发点正确

## GUI系统检查

- [ ] PySide6与asyncio正确桥接
- [ ] 信号槽连接正确
- [ ] 状态变更实时反映
- [ ] QSS样式系统工作

## 代码质量检查

### 类型安全

- [ ] 所有函数有参数类型注解
- [ ] 所有函数有返回类型注解
- [ ] 类属性有类型注解
- [ ] 无 `Any` 类型滥用

### 异步规范

- [ ] I/O操作使用 `async/await`
- [ ] 数据库操作使用 `aiosqlite` 或 `async` SQLAlchemy
- [ ] 无阻塞调用（如 `time.sleep`）
- [ ] GUI事件通过 `qasync` 桥接

### 错误处理

- [ ] 使用异常而非返回码
- [ ] 有适当的异常类型
- [ ] 关键操作有 `try/except`
- [ ] 资源释放使用 `finally` 或上下文管理器

### 代码风格

- [ ] 通过Black格式检查
- [ ] 通过Ruff静态检查
- [ ] 文档字符串Google Style
- [ ] 行宽不超过100字符
